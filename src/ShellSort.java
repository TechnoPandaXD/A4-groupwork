
public class ShellSort {

	public static void sort(IntBoxNode[] boxes, SortingAnimation screen) {
		int n = boxes.length;
		
		for (int gap = (n / 2); gap > 0; gap /= 2) {
			
			for (int i = gap; i < n; i++) {
				
				//TODO: sorting logic here + animation calls
			}
		}
	}
	
	private static boolean greater(IntBoxNode a, IntBoxNode b) {
		//TODO:
		return false;
	}
	
	private static void shift(IntBoxNode[] boxes, int from, int to, SortingAnimation screen) {
		//TODO:
	}
}
